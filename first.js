function outside(){

    //string
    var name='林子';
    var age=20;
    var message='hello,'+name+'how old are you'+age+'?';//combine string

    //array
    var arr=['a','b','c','d','e'];
    arr.slice(0,3);
    console.log(arr.slice(0,3));
    
    // alert('hello world');

    console.log(message.length);
}

outside();
